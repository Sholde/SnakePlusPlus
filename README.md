# Snake Plus Plus

## Using

### Debian

- `./cmake_build.sh` : to build the project
- `./compile.sh` : to compile the project
- `./run.sh` : to run the project

## Control

```
  Z
Q S D
```

- `A` : quit